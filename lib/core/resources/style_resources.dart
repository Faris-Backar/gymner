import 'package:flutter/material.dart';

class StyleResources {
  StyleResources._();
  static const accentColor = Colors.white;
  // static const primaryColor = Color(0xFF3751FF);
  static const primaryColor = Color(0xFFFF8A08);
  static const secondaryColor = Color(0xFF1C315E);
  static const errorColor = Colors.red;
  static Color scaffoldBackgroundColor = Colors.white.withOpacity(.8);
  static const black = Color(0xFF151515);
  static const greyBlack = Color(0xFF322C2B);
}
